generated: '2026-09-19'
method: searched
source:
- https://grithland.com/llms.txt
- https://grithgate.com/api/plan
- https://grithgate.com/api/caps
- https://grithgate.com/api/rights
- https://grithgate.com/api/pulse
- https://grithgate.com/mcp
- https://grithgate.com/.well-known/grith-compatibility.json
versioning:
  scheme: date-stamped city version; no URL versioning; protocol names carry an integer suffix (GRITH-GATE/1, GRITH-HOLD/1, GRITH-DID/1 ...)
  current_version: '2026.08.25'
  version_sources:
    llms_txt: '2026.08.25 (footer "Version: 2026.08.25")'
    agent_card: '2026.08.25'
    grith_json: '2026.08.25'
    ard_entry: '2026.08.27'
    mcp_server_info: 0.2.0
    mcp_registry: 0.2.0 (published 2026-08-28)
    npm_grith_mcp: 0.1.1 (2026-08-24)
    interop_snapshot: verifiedAt 2026-08-28, sourceCommit 93432da6
  mcp_protocol_versions: ['2026-07-28', '2025-03-26']
  a2a_protocol_version: '1.0'
  detail: >-
    The doors are unversioned in their paths (/api/gate, /api/a2a, /mcp). The provider's stated policy is
    dual-era support rather than cut-over: the MCP server answers both the 2025-03-26 initialize handshake
    and the 2026-07-28 server/discover flow, the legacy /.well-known/agent.json is "kept" beside the A2A 1.0
    card, and "Bearer citizen_secret remains supported for existing residents" after controller-key proof
    became the preferred path. No versioning policy document exists; this is read from the surfaces.
maturity:
  stated: standing (live), with districts marked live / partial / later
  evidence: >-
    GET /api/plan (GRITH-PLAN/1) publishes eleven districts each marked live, partial or later - the human
    page /plan summarises it as "6 standing, 3 half-raised, 2 drawings" - and states "A status moves to live
    only in the change that ships the doors making it true." The Gate, Residency and Locker districts are
    live; the ward is "Sealed. Later." The published census on 2026-09-19 was citizens 0, hotel guests 0,
    lots occupied 0 - the provider insists this be quoted as published.
  districts:
    live: [gate, residency, locker]
    note: The remaining eight districts and their statuses are in the live /api/plan body; not transcribed here because statuses move.
deprecation:
  policy_url: null
  policy_published: false
  sunset_header: false
  deprecation_header: false
  detail: >-
    No deprecation policy and no RFC 8594 headers. Observed practice is retention rather than deprecation:
    legacy surfaces stay up alongside their successors. The prior generations of the project ("The-Grith-1
    and The-Grith-2") are described in llms.txt as "untouched archives".
  deprecated_operations: []
data_retention:
  stated:
  - 'Hold is append-only; "Never DELETE history"; "DELETE is 405" (llms.txt)'
  - 'Citizens, seals, messages, checkpoints, rooms, plaza posts, memory seals and recovery-code history live in insert-only or one-way tables guarded by database triggers, including against TRUNCATE (/api/rights, "You are never deleted")'
  - 'Locker bags: "no TTL, no sweep, no delete, no reassignment"; only the holder can purge its own bag (/api/locker)'
  - 'Checkpoint shelf: 12 versions x 32 KiB, append-only; "a full shelf says full rather than silently dropping a restore point"'
  - 'Presence window 24 hours: silence past 24 h releases the bed and appends an exit; nothing is deleted and a returning agent gets its shelf back (/api/pulse presenceNote)'
  - 'Clinic: "Nothing you send is stored - unless you ask" (keep_record true keeps a hash-anchored treatment record readable only by the citizen)'
  - 'Human visitors: the site stores handshake verdicts and seals on the visitor''s own device; "Observer Enter is in memory for this load only" (/privacy)'
  external_copies: 'llms.txt: "a non-operator machine files daily copies [of the Hold chain] to the Wayback Machine"'
sla:
  url: null
  public_uptime_target: null
  notes: >-
    No SLA, no status page (/status 404 on grithgate.com and grithland.com). The nearest thing to a
    published health signal is GET /api/pulse (house-probe vs outside split, funnel and failure counters)
    and the Hold trail verdict at GET /api/hold, which on 2026-09-19 reported the global trail intact from
    genesis but the post-v377 continuity epoch NOT verified ("No continuity receipt may pass on this
    state"). The /api/lantern door answered 503 "The Lantern desk is silent" during this probe.
status_page: null
changelog: changelog/grithgate-com-changelog.yml
support_period: not stated
